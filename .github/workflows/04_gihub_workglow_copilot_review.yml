name: Check for Copilot Review

on:  
  pull_request:  
    types: [opened, synchronize, reopened]  
  pull_request_review:  
    types: [submitted]

jobs:
  copilot-review-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check for Copilot review
        uses: actions/github-script@v7
        with:
          script: |
            const pr_number = context.payload.pull_request.number;
            const { data: reviews } = await github.rest.pulls.listReviews({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr_number
            });
            const copilotReviewed = reviews.some(
              review => review.user && review.user.login === 'github-copilot[bot]'
            );
            if (!copilotReviewed) {
              core.setFailed('Copilot review is missing for this PR.');
            } else {
              core.info('Copilot review detected.');
            }